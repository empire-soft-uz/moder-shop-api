import IFormatedProps from "../Interfaces/Product/IFormaterProps";
import { IPropValue } from "../Interfaces/Product/IPropValue";
export default class PropFormater {
    static format(props: IPropValue[]): IFormatedProps[];
}
